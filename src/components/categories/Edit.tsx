import React from 'react';

const Edit = () => {
  // function createLabel(nameOrId: string | number): IdLabel | NameLabel;
  // function createLabel(nameOrId: string | number): IdLabel | NameLabel {}

  return <div>Edit</div>;
};

export default Edit;
